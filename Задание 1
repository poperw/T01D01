git clone https://github.com/aidarmar123/T01D01.git
git checkout -b develop
git checkout develop
git checkout -b key-branch
echo "main-key-content" > main.key
cp main.key main-2.key
git add main.key main-2.key
git commit -m "Добавлены main.key и main-2.key"
git checkout -b feature/3-key
cp main.key main-3.key
git add main-3.key
git commit -m "feature/3-key: Добавлен main-3.key"
git checkout key-branch
git merge feature/3-key
git checkout develop
git checkout -b release/1.0
# Подливаем key-branch в develop
git checkout develop
git merge key-branch

# Подливаем develop в release/1.0
git checkout release/1.0
git merge develop
git log --oneline --graph --all
